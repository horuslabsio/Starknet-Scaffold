export interface WikipediaResource {
  id: string;
  name: string;
  description: string;
  status: string;
  url: string;
  category: string;
}

export interface AddressBookResource {
  id: string;
  name: string;
  description: string;
  address: string;
}
